# Phase 53C 分析笔记：√{k(k+1)} 的谱推导

> 日期：2026-07-21
> 关联：Phase 53 路线图，CategoryRepBridge.lean, SpectralGap.lean

---

## 1. 问题陈述

**目标**：证明 `agEigenvalue(k, k_max)`（`SpectralGap.lean`）给出的 `√{k(k+1)}/√{k_max(k_max+1)}` 是 SU(2) 自旋 j 表示中 Casimir 算子的归一化特征值，其中 `j = k/2`，`j_max = k_max/2`。

**当前状态**：
- 53B 已证明 2×2 情况（j=1/2）：C₂ = (3/4)·I，特征值 = {3/4} ✅
- 需要推广到任意 j（即任意 k_max）的 (2j+1)×(2j+1) 表示

### 1.1 核心公式

SU(2) 的 spin-j 表示中，Casimir 算子 C₂ 是标量矩阵：

$$C_2 = j(j+1) \cdot I_{2j+1}$$

其中 $I_{2j+1}$ 是 (2j+1)×(2j+1) 单位矩阵。令 $j = k/2$, $j_{\max} = k_{\max}/2$：

$$\frac{\|C_2(j)\|}{\|C_2(j_{\max})\|} = \frac{\sqrt{j(j+1)}}{\sqrt{j_{\max}(j_{\max}+1)}} = \frac{\sqrt{k(k+1)/4}}{\sqrt{k_{\max}(k_{\max}+1)/4}} = \frac{\sqrt{k(k+1)}}{\sqrt{k_{\max}(k_{\max}+1)}}$$

这正是 `agEigenvalue(k, k_max)`。

### 1.2 证明策略

对于一般的 j，直接构造 (2j+1)×(2j+1) 自旋矩阵并证明 C₂ = j(j+1)·I。

**自旋矩阵定义**（J_z 是对角的）：
- $(J_z)_{m,m'} = m \cdot \delta_{m,m'}$，其中 $m = -j, -j+1, \dots, j$
- $(J_+)_{m,m+1} = \sqrt{(j-m)(j+m+1)}$
- $(J_-)_{m-1,m} = \sqrt{(j-m+1)(j+m)}$

**Casimir 恒等式**：
$$C_2 = J_x^2 + J_y^2 + J_z^2 = J_z^2 + \frac{1}{2}(J_+J_- + J_-J_+) = j(j+1) \cdot I$$

### 1.3 Lean 实现方案

对于有限小的 j 值（j = 0, 1/2, 1, 3/2, 2, ...），可以显式构造矩阵并直接计算证明。对于一般 j，引用标准表示论结论。

本阶段实现 j = 0, 1/2, 1 的显式证明，并陈述一般定理。

| j | k | 维数 | C₂ 值 | 状态 |
|:-:|:-:|:----:|:-----:|:----:|
| 0 | 0 | 1 | 0 | 待添加 |
| 1/2 | 1 | 2 | 3/4 | ✅ 53B 已证明 |
| 1 | 2 | 3 | 2 | 待添加 |
| 3/2 | 3 | 4 | 15/4 | 公式陈述 |
| 2 | 4 | 5 | 6 | 公式陈述 |
| 一般 j | — | 2j+1 | j(j+1) | 公式引用 |
