# Phase 57：LeaverUnifiedSolver 独立数值工具规划

**状态**：路线图（远期规划，未启动）

**目标**：将 `LeaverUnifiedSolver` 从 MUFPF 框架内的验证工具升级为面向引力波社区的独立可安装 Python 包，与现有 `qnm` 包形成互补。

---

## 一、战略定位

### 与 `qnm` 包的差异化定位

| 维度 | `qnm` 包 | 本求解器 |
|:----|:---------|:--------|
| 核心算法 | 标准 Leaver CF + 角向矩阵法 | 谱化理论 + 双重同伦(a+m) + LACI + 双初始向量逆迭代法 |
| Kerr 高自旋 | Newton 迭代，初始猜测敏感 | 双重同伦延拓，自动避开非物理根 |
| 物理根选择 | 手动 | LACI 自动选择 |
| 角向-径向耦合 | 分步 | 集成自洽（矩阵角向 + 径向多项式） |
| 加速策略 | 无 | 双初始向量逆迭代法 O(N) 谱残差 |
| Berti 表偏差 | 未处理 | COOK_REF_TABLE 显式替代 |

### 目标用户

- GW 数据分析研究者（需批量 QNM 频率做模板库）
- 高自旋/极端 Kerr 物理研究者
- 教育/教学中的 QNM 计算演示

---

## 二、阶段划分（远期计划）

### 第一阶段：工程化（目标 1-2 周集中投入）

| 任务 | 描述 | 预期产出 |
|:----|:----|:--------|
| 1.1 包结构 | `pyproject.toml`, 模块分离, `__init__.py` | 可 `pip install -e .` |
| 1.2 API 清理 | 统一求解器接口，移除 MUFPF 内部依赖 | `qnm_solver.LeaverKerrSolver` |
| 1.3 依赖简化 | 仅依赖 `numpy`, `scipy`，移除 MUFPF 内部引用 | 轻量安装 |
| 1.4 文档 | 英文 README + 标准 API 文档 | 社区可读 |
| 1.5 示例 | Jupyter notebook（Schwarzschild + Kerr 快速入门） | 用户上手材料 |

### 第二阶段：验证与基准（目标 1 周集中投入）

| 任务 | 描述 | 预期产出 |
|:----|:----|:--------|
| 2.1 全模式扫描 | a ∈ [0, 0.99], l ∈ [2, 4], m ∈ [-l, l], n ∈ [0, 2] | 覆盖 Berti 表全部范围 |
| 2.2 精度对比 | 与 `qnm` 包逐点对比（除系数约定差异外） | 精度统计报告 |
| 2.3 速度对比 | 不同自旋/模式下的 Wall time 对比 | 加速比报告 |
| 2.4 收敛性分析 | 截断 N 对精度的影响，推荐默认值 | 使用指南 |

### 第三阶段：发布（持续）

| 任务 | 描述 |
|:----|:----|
| 3.1 PyPI 发布 | `pip install kerr-qnm-spectral` |
| 3.2 软件论文 | JOSS 或 CPC 简短论文（2-4 页） |
| 3.3 社区维护 | Issue 跟踪、CI、版本管理 |

---

## 三、关键决策点

### 包名与命名空间

候选：`kerr-qnm-spectral`, `spectral-qnm`, `leaver-unified`

### MUFPF 内部依赖处理

当前 `LeaverUnifiedSolver` 引用了 MUFPF 框架的：
- `DerecursionAnalyzer`（谱化理论核心）
- `LACIEvaluator`（LACI 判据）
- `TridiagonalSpectralSolver`（双初始向量逆迭代法）

**方案 A**（推荐）：将这三个类提取为独立模块，剥离 MUFPF 范畴论表述，保留数值算法核心。

**方案 B**：直接发布完整的 `leaver_unified_solver.py`，保留内部引用但允许 `pip install`。

### 参考表分发

`COOK_REF_TABLE` 作为初始猜测参考和验证基准，应包含在包内。需明确标注此表是"本框架自洽追踪值，非 Berti (2006) 表值"。

---

## 四、迭代式推进计划

```
时间线     Phase 57A（工程化，~2周）          Phase 57B（验证基准，~1周）       Phase 57C（发布，持续）
         ┌──────────────────────────┐     ┌──────────────────────────┐     ┌────────────────────────┐
         │ 包结构搭建                │     │ 全模式精度-速度基准       │     │ PyPI 发布               │
         │ API 清理 + 依赖分离       │     │ 与 qnm 系统对比           │     │ JOSS 软件论文           │
         │ 英文文档 + Jupyter 示例   │     │ 收敛性分析 + 使用指南     │     │ 社区维护                │
         └──────────────────────────┘     └──────────────────────────┘     └────────────────────────┘
```

---

## 五、不做的范围

- **不实现** Kerr 以外的背景（RN、dilaton 等）——保持专注
- **不实现** 角向模式的自动分类（l,m 模式识别）——留给用户
- **不实现** 全波形合成——留给 Paper XXVI 集成
- **不实现** GPU 加速——当前 CPU 实现足够

---

## 六、前置条件

- [ ] `LeaverUnifiedSolver` 在 Kerr 模式下稳定（当前已基本完成）
- [ ] 与 `qnm` 包的系统性精度对比完成（至少覆盖主要模式空间）
- [ ] 明确求解器的"核心创新点"表述（作为论文背景）

**当前状态**：前置条件基本满足，仅系统性精度对比尚未正式执行。
